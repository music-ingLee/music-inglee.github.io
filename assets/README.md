# assets/

Drop your real files here — the page picks them up automatically:

- `profile.jpg` — your profile photo. While it's missing, `avatar-placeholder.svg` is shown instead.
- `cv.pdf` — your resume. The **View CV** button links to it.

> Note: files in a public repo are publicly downloadable. If you don't want that, keep using placeholders.
